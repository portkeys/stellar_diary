# NASA API Key
# Get your free NASA API key at https://api.nasa.gov/
# Without a key, the API uses DEMO_KEY which has strict usage limits
NASA_API_KEY=DEMO_KEY

# Server port (default is 5000)
PORT=5000